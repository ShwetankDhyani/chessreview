# Optional: Gemini API for AI coach commentary (set in Vercel → Settings → Environment Variables)
VITE_GEMINI_API_KEY=

# Optional: remote Stockfish HTTP server (e.g. self-hosted). Leave empty on Vercel — uses Lichess + browser engine.
# VITE_EVAL_SERVER_URL=https://your-eval-server.example.com
