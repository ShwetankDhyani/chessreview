# Optional: Gemini API for AI coach commentary
VITE_GEMINI_API_KEY=

# Native Stockfish server (Fedora). Required for full fast analysis on Vercel:
# 1) npm run eval-server:public   (on Fedora)
# 2) npm run expose-engine        (copy https URL)
# 3) Set same URL in Vercel → Environment Variables
# VITE_EVAL_SERVER_URL=https://your-tunnel.trycloudflare.com
